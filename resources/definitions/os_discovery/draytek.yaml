mib: DRAYTEK-MIB
modules:
    os:
        sysDescr_regex: '/Router Model: (?<hardware>[\w ]+), Version: (?<version>[\w\.]+)/'
    mempools:
        data:
            -
                percent_used: .1.3.6.1.4.1.7367.3.7.0
    sensors:
        count:
          data:
            -
              oid: lteactivechannel
              num_oid: ".1.3.6.1.4.1.7367.4.1.{{ $index }}"
              descr: "LTE Channel modem {{ $index }}"
              group: Mobile
              index: "lteactivechannel.{{ $index }}"
              
                     
