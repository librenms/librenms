modules:
    os:
        sysDescr_regex: '/SEH (?<hardware>\S+); SN (?<serial>\S+), .* SW (?<version>\S+)/'
    sensors:
       state:
           data:
               -
                   oid: SEH-PSRV-MIB::utnPortTable
                   value: SEH-PSRV-MIB::utnPortSlot
                   num_oid: '.1.3.6.1.4.1.1229.2.50.2.1.27.{{ $index }}'
                   descr: 'Port{{ SEH-PSRV-MIB::utnPortIndex }}: {{ SEH-PSRV-MIB::utnPortUsbOwn }}'
                   group: USB Port
                   index: 'utnPortIndex.{{ $index }}'
                   skip_values:
                   -
                      oid: SEH-PSRV-MIB::utnPortTag
                      op: 'exists'
                      value: false
                   state_name: utnPortSlot
                   states:
