$(document).ready(function(){
	
	/*
		$("img.lazy").lazyload({
    		effect: "fadeIn",
    		threshold: 200
    }).removeClass("lazy");

    $(document).ajaxStop(function() {
        $("img.lazy").lazyload({
            effect: "fadeIn",
    		    threshold: 200
        }).removeClass("lazy");
    });
*/
});