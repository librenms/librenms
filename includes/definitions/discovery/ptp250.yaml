modules:
  os:
    sysDescr_regex: '/5\d(?<hardware>.*?)( Wireless|,).*Version\s+(?<version>[\d-]+)/'
    hardware: CAMBIUM-PTP250-MIB::masterSlaveMode.0
    hardware_template: '{{ $hardware }} {{ CAMBIUM-PTP250-MIB::masterSlaveMode.0 }}'
