os: merakiz
text: 'Meraki Z series Appliance'
type: appliance
icon: meraki
ifname: true
over:
    - { graph: device_bits, text: 'Device Traffic' }
discovery:
    - sysDescr_regex:
        - '/^Meraki Z/'
